# CLAUDE.md

See **[AGENTS.md](./AGENTS.md)** for the full working guide — stack,
commands, repository layout, content conventions, theming, and quality
gates. It is the single source of truth for this repository.

Quick reminders:

- Package manager is **pnpm**.
- Run `pnpm format && pnpm lint && pnpm typecheck && pnpm build` before
  pushing — CI enforces all four.
- Content is Markdown under `docs/`; the sidebar is autogenerated from the
  folder structure.
- Do not hand-edit `.claude/` or `.planning/` (GSD-managed).
